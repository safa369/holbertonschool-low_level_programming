#include "main.h"
/**
 * main - function print the name of the program.
 * @argc: int;
 * @argv: char;
 * Return: always 0.
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
